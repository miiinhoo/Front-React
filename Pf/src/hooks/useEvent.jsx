import { useState } from "react";

const useEvent = () => {
  const [ open, setOpen ] = useState(false);

  const openSkill = () => {
    setOpen(true);
    
  }


  return{

  }
}
export default useEvent;